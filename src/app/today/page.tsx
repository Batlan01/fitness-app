export default function Today(){
  // Placeholder: később program alapján "mai edzés"
  return (
    <main className="grid">
      <div className="card">
        <h2 className="text-xl font-semibold">Mai edzés</h2>
        <p className="text-[var(--muted)]">Később itt jelenik meg a kiválasztott program napi edzése.</p>
      </div>
    </main>
  );
}
